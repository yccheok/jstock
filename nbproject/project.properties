application.args=
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/jstock.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
file.reference.jstockhelp.jar=help/jstockhelp.jar
includes=**
jar.compress=false
javac.classpath=\
    ${libs.logging.classpath}:\
    ${libs.httpclient.classpath}:\
    ${libs.codec.classpath}:\
    ${libs.JGoodiesForms.classpath}:\
    ${libs.l2fprod.classpath}:\
    ${libs.SwingX.classpath}:\
    ${libs.JFreeChart.classpath}:\
    ${libs.JHotDraw-Unstable.classpath}:\
    ${libs.swing-layout.classpath}:\
    ${libs.xstream.classpath}:\
    ${libs.nachocalendar.classpath}:\
    ${libs.JavaMail.classpath}:\
    ${libs.commons-net.classpath}:\
    ${libs.poi.classpath}:\
    ${libs.JavaHelp.classpath}:\
    ${file.reference.jstockhelp.jar}
# Space-separated list of extra javac options
javac.compilerargs=-Xlint:unchecked
javac.deprecation=false
javac.source=1.6
javac.target=1.6
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
# Property libs.codec.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.codec.classpath=../../commons-codec-1.3/commons-codec-1.3.jar
# Property libs.commons-net.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.commons-net.classpath=../../commons-net-1.4.1/commons-net-1.4.1.jar
# Property libs.httpclient.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.httpclient.classpath=../../commons-httpclient-3.1-rc1/commons-httpclient-3.1-rc1.jar
# Property libs.JavaHelp.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JavaHelp.classpath=../../jh2.0/javahelp/lib/jh.jar
# Property libs.JavaMail.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JavaMail.classpath=../../javamail-1.4/mail.jar
# Property libs.JFreeChart.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JFreeChart.classpath=../../jfreechart-1.0.5/lib/jcommon-1.0.9.jar:../../jfreechart-1.0.5/lib/jfreechart-1.0.5.jar
# Property libs.JGoodiesForms.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JGoodiesForms.classpath=../../forms-1.1.0/forms-1.1.0.jar
# Property libs.JHotDraw-Unstable.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.JHotDraw-Unstable.classpath=../../trunk/jhotdraw7/dist/JHotDraw7.jar
# Property libs.l2fprod.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.l2fprod.classpath=../../l2fprod-common-7.3/lib/l2fprod-common-all.jar
# Property libs.logging.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.logging.classpath=../../commons-logging-1.1/commons-logging-1.1.jar
# Property libs.nachocalendar.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.nachocalendar.classpath=../../nachocalendar-0.23/lib/nachocalendar-0.23-demo.jar:../../nachocalendar-0.23/lib/nachocalendar-0.23.jar
# Property libs.poi.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.poi.classpath=../../poi-3.0.1-FINAL/poi-3.0.1-FINAL-20070705.jar
# Property libs.xstream.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.xstream.classpath=../../xstream-1.2.1/lib/cglib-nodep-2.1_3.jar:../../xstream-1.2.1/lib/dom4j-1.6.1.jar:../../xstream-1.2.1/lib/jdom-1.0.jar:../../xstream-1.2.1/lib/joda-time-1.2.1.jar:../../xstream-1.2.1/lib/stax-1.2.0.jar:../../xstream-1.2.1/lib/stax-api-1.0.1.jar:../../xstream-1.2.1/lib/xml-writer-0.2.jar:../../xstream-1.2.1/lib/xom-1.1.jar:../../xstream-1.2.1/lib/xpp3_min-1.1.3.4.O.jar:../../xstream-1.2.1/lib/xstream-1.2.1.jar:../../xstream-1.2.1/lib/xstream-benchmark-1.2.1.jar
main.class=org.yccheok.jstock.gui.MainFrame
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
platform.active=default_platform
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
src.dir=src
test.src.dir=test
